# Web Search Tools

This module provides Tavily-backed MCP tools for:

- `web_search`
- `web_get_content`
- `web_research`

To use these tools, save a Tavily API key from the web UI `Account` modal. The key is read from the local runtime settings store instead of a local env file.
